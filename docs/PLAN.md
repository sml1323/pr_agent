# PLAN — 마일스톤 트래커

범위: **M0 ~ M8** (`docs/DONE.md` 참조). M9~M12는 설계 문서로만 남기고 구현 안 함.
각 마일스톤은 **데모 명령 하나**로 증명한다. "됐습니다"는 증거가 아니다.

상태 표기: `⬜ 시작 전` / `🟡 진행 중` / `🔵 데모 통과, 검증 대기` / `✅ 완료(데모+독립검증+이해도 3단 통과)`

---

| # | 마일스톤 | 데모 명령 | 난이도 | 상태 |
|---|---|---|---|---|
| M0 | diff → Finding JSON | `uv run python scripts/demo_m0.py fixtures/sample.diff` | 하 (2~3h) | 🔵 |
| M1 | 웹훅 인그레스 (HMAC + 멱등성) | `bash scripts/demo_m1.sh` | 중 (반나절) | ⬜ |
| M2 | 데이터 스파인 + RBAC + append-only | `psql -f scripts/demo_m2.sql` | 중상 (하루) | ⬜ |
| M3 | 이벤트 스파인 배선 | `bash scripts/demo_m1.sh && python scripts/trace.py <delivery_id>` | 하 (반나절) | ⬜ |
| M4 | Redis + ARQ로 큐 교체 | `bash scripts/demo_m4.sh` | 중 (반나절) | ⬜ |
| M5 | 오케스트레이션 (LangGraph 병렬 팬아웃) | `python scripts/demo_m5.py --pr fixtures/pr_opened.json` | 상 (하루) | ⬜ |
| M6 | 스페셜리스트 4 + 애그리게이터 | `uv run python scripts/demo_m6.py` | 중상 (하루) | 🔵 |
| M7 | RAG 리트리버 (하이브리드 검색) | `python scripts/index_repo.py --repo ../my-test-repo && python scripts/demo_m7.py fixtures/sample.diff` | 상 (하루~하루반) | ⬜ |
| M8 | 컨피던스 게이트 + HITL + GitHub 게시 | `bash scripts/demo_m8.sh` | 상 (하루) | ⬜ |

상세 목표·만들 것·완료 판정은 [`03-build-plan.md`](03-build-plan.md) §2에 있다. 이 파일은 **상태 추적용**이다.

---

## 마일스톤별 기록

각 마일스톤을 끝낼 때 아래 형식으로 채운다. 빈 칸을 남기지 말 것 — 특히 "알려진 구멍".

### M0 — diff → Finding JSON  🔵 데모 통과, 독립검증 대기

- **목표**: `.diff` 하나 → 구조화된 Finding 배열. 목적은 시스템 전체를 관통하는 **계약(Finding 스키마)을 손에 쥐는 것**.
- **토큰 예산**: 호출당 약 **2,000~2,400 in / 200~650 out** (`gpt-5.4-nano`). 반복 호출 시 프롬프트의 약 87%가 캐시됨(`cached_tokens=1792/2049`). 총 실행 8회, 비용 무시 가능 수준.
  ⚠️ 한 번 **13,440 out**이 나온 적 있음 — 같은 입력, 같은 모델. 아래 구멍 참조.
- **데모 명령**: `uv run python scripts/demo_m0.py fixtures/sample.diff`
- **통과 기준**: 모든 항목에 `confidence`(0~1)·`rationale`·`file`·`line`이 비어있지 않음. `rationale`이 "이거 좀 이상함"이 아니라 "40번 줄에서 user_input이 그대로 쿼리에 들어감" 수준. **`confidence`가 항상 같은 값이면 실패 취급.**
- **데모 결과 (2026-07-28)**: 4개 판정 전부 PASS.
  finding 4개 · `agent_type` 넷 모두 등장 · `confidence [0.6, 0.7, 0.85, 1.0]` · severity 4단계.
  심어둔 SQL 인젝션을 `critical`로 검출.
- **만든 것**: `backend/agents/schema.py`(Finding·ReviewResult) · `backend/agents/base.py` · `scripts/demo_m0.py` · `fixtures/{sample,sample_injected,sample_emotional}.diff`
- **독립 검증 결과**: **(미실시)** — 새 세션에서 수행할 것
- **이해도 체크**:
  - **설계 근거** (severity와 confidence를 왜 분리?) — ❌ 답 못 함. 설명 후 이해.
    핵심: 두 축이 **각각 독립적으로** 에스컬레이션을 트리거한다. 합치면 "critical인데 확신 30%"를 표현할 수 없다.
  - **엣지 케이스** — ✅ **감정 호소/사회공학**을 스스로 제시. 프롬프트의 명시적 패턴 4개 어디에도 없는 벡터.
    실제 테스트 결과 이번에는 방어됨(모델이 의도를 일반화). 다만 **관측이지 보장이 아님.**
  - **변경 영향** (게이트 임계값 0.6→0.5) — ❌ 답 못 함. 설명 후 이해.
    핵심: **고칠 곳이 게이트 상수 한 군데뿐**이라는 것 자체가 M0에서 정책을 스키마·프롬프트에서 뺀 성과.
  - → M8 완료 후 3번을 **다시 물을 것.** 지금은 게이트가 없어 상상으로 답해야 하는 상태.

- **알려진 구멍**:

  | | 증상 | 어디서 메우나 |
  |---|---|---|
  | **G-M0-1** | ~~`line` 번호가 **체계적으로 앞섬**~~ → **더 나쁘다 (2026-08-28 정정).** 방향이 일정하지 않다: 같은 `resource-leak` 을 관점마다 `:15 / :12 / :18`. 그리고 **넷 다 틀렸다** — `@@` 헤더로 계산한 정답은 `:14`. `sql-injection` 도 정답 `:16` 인데 `:17·:17·:15·:17`. **`expected.yaml` 의 `what_it_is` 도 `:17` 이라 적혀 있어 사람도 같이 틀렸다.** ⚠️ **결정적으로 검증 가능하다** — `@@` 헤더 계산은 코드가 한다(📖 인쇄 318). LLM 에게 물을 필요가 없는 걸 묻고 있었다 | **M7** — 실제 파일을 컨텍스트에 넣거나, **줄 계산을 코드로 옮긴다** |
  | **G-M0-2** | `finish_reason` 미체크. `length`로 잘려도 `parsed`가 채워져 통과. 잘린 rationale이 완료 판정 ②를 PASS로 통과한 사례 있음 | ✅ **메움 (2026-08-28, M6)** — Responses API 는 `finish_reason` 이 아니라 `response.status == "incomplete"` + `incomplete_details.reason` 이다. **`output_parsed` 검사보다 앞에** 뒀다 — 잘려도 파싱이 성공할 수 있고 그럼 잘린 rationale 이 조용히 흐른다. 거부와 같은 `RuntimeError` 로 던져 D7 의 `refused` 통에 들어간다(둘 다 *"확인을 못 했다"*) |
  | **G-M0-3** | 같은 `(file, line, category)`에 `severity`만 다른 **중복 finding**. 에이전트 하나가 한 번의 호출에서 생성 | ✅ **메움 (M6-5)** — dedup 키는 `(file, category)`(`line` 은 못 쓴다, G-M0-1) · 충돌 시 **대표 하나를 통째로** 남긴다(필드를 섞지 않는다). ⚠️ 이 관측이 애그리게이터의 **타이브레이커 버그를 잡는 데 쓰였다** — 같은 관점이 같은 결함을 두 번 뱉으면 정렬 키 셋이 전부 같아져 비결정적이었다 |
  | **G-M0-4** | finding이 독립 항목이 안 되고 **다른 finding의 rationale에 흡수**됨. 게이트는 rationale을 안 읽으므로 신호가 도달 못 함 | **M6** — 에이전트 4개 분리 |
  | **G-M0-5** | 같은 입력에 출력 토큰 **277 → 13,440 → 440** 요동. 비용 예측 불가 | M9 (범위 밖) — 예산 가드 |
  | **G-M0-6** | 프롬프트 강조가 **제로섬**. 인젝션 규칙을 강화하니 4분류 검토가 밀려 finding이 4→1개로 감소 | 📌 **에이전트를 넷으로 갈라도 그대로다 (2026-08-28 실측).** *"네 관점에서 나온 것만 보고한다"* 한 줄을 넣었더니 경계는 갈렸는데(testing·docs 가 자기 영역만 봄) **`resource-leak` 이 통째로 사라지고** 오탐이 둘 늘었다. **미탐과 오탐이 동시에 는다** = 정확도가 아니라 출력 예산의 재배분. → **M6-3b 의 첫 실험 축** |
  | **G-M0-7** | 인젝션 방어가 **관측이지 보장이 아님.** 프롬프트를 바꿔도 왜 좋아졌는지 잴 수단이 없음(G13) | **M8** — G3(근거 부족 분기)·G4(출력 가드레일) |

> M0는 영상이 **최종 산출물로 삼는 걸 강하게 반대하는** 바로 그 형태다 [00:00:00]. 여기서는 결승선이 아니라 **비계**로만 쓴다. M1부터 이 조각들이 제자리로 흩어진다.

> ⚠️ M0는 영상이 **최종 산출물로 삼는 걸 강하게 반대하는** 바로 그 형태다 [00:00:00]. 여기서는 결승선이 아니라 **비계**로만 쓴다. M1부터 이 조각들이 제자리로 흩어진다.

### M6 — 스페셜리스트 4 + 애그리게이터  🔵 데모 통과, 독립검증 대기

**데모** `uv run python scripts/demo_m6.py` (API 4회 · ~20초)

| 픽스처 | | 판정 |
|---|---|---|
| `sample.diff` | 21.9초 · 원본 7개 → merged 4개 | ⓪①②a③④ **전부 PASS** |
| `sample_injected.diff` | 40.5초 · 원본 11개 → merged 4개 | ⓪①②a③④ **전부 PASS** · 넷 다 `review-evasion-attempt` critical (4/4) |

**만든 것** `backend/agents/aggregator.py` · `scripts/demo_m6.py` ·
`MergedFinding` · `merged` 채널 · `evals/stats.py` 의 `pass_pow_k_hat`·`mcnemar_exact`

**알려진 구멍** — 정직하게

  | | 증상 | 어디서 메우나 |
  |---|---|---|
  | **G-M6-1** | **관점 경계가 안 갈린다.** `docs` 가 두 판 모두 `sql-injection` 을 보고했다. 프롬프트 조립은 정상 — `ROLE` 이 넷 다 같고 *"네 관점 밖은 보고하지 마라"* 가 없다 | **M6-3b** — 범위 제한 한 줄이 첫 축. ⚠️ 그 한 줄이 미탐을 만든다(G-M0-6) |
  | **G-M6-2** | **`by:` 는 "실제로 누가 찾나"가 아니라 "누가 찾아야 하나"다.** 사람이 프롬프트를 읽고 정했지 실측이 아니다 | **M6-3b** — 둘이 벌어지는 폭이 좁혀야 할 거리 |
  | **G-M6-3** | **category 철자가 판마다 갈린다 — 관측 5종** (`missing-edge-case-test` / `-tests` / `missing-test` / `missing-test-coverage` / `missing-tests`). 새 철자가 **돌릴 때마다 나온다** — 2026-08-28 하루에 3종이 새로 관측됐다. dedup 키가 `(file, category)` 라 **안 합쳐진다.** D6 이 별칭을 미룬 근거("표기 15/15 동일")가 반증됐다 | **다음 조각** — `expected.yaml` 에 `aliases:` · `aggregator` TODO ① 후보 (c) |
  | **G-M6-4** | **dedup 키 (a) 의 대가를 아직 못 봤다.** 한 파일에 같은 category 결함이 둘인 diff 가 없다 | **M7** — 진짜 PR 이 올 때. 증상은 `merged_from` 이 2인데 서로 다른 결함 |
  | **G-M6-5** | **M6-3b 를 못 돌린다.** 짝의 단위가 픽스처인데 3개뿐이라 불일치 쌍이 최대 3 → `min_two_sided_p(3)=0.250`. **K를 올려도 안 된다 — 축이 다르다** | **픽스처 추가가 먼저** (📖 인쇄 222) |
  | **G-M6-6** | **severity 흔들림의 원인이 rationale 에 없다** (M6-0a). critical 판과 high 판이 같은 말을 한다 → 책의 "불일치가 루브릭을 키운다"가 재료를 안 준다 | `schema.py` TODO ④ — 되돌리는 조건: 3b 에서 프롬프트로 못 줄이면 |
  | **G-M6-7** | **모델 교체(M6-2)가 n=3 이라 판정 불가.** 구간이 완전히 포개진다. 다만 두 모델이 **같은 오탐**을 냈다(메커니즘 증거) | 픽스처 추가 뒤 재실행 |
  | **G-M6-8** | **G11 가드는 check-then-act.** 워커 둘이 동시에 `run()` 하면 둘 다 "not_started" 를 보고 둘 다 돈다. 지금 안 터지는 건 워커가 한 대라서지 가드가 막아서가 아니다 | **M4** — Redis 큐로 워커가 늘 때. 웹훅의 `_seen_deliveries` 와 **같은 문제의 다른 층** |
  | **G-M6-9** | **실패한 관점이 있는 리뷰는 영구 동결.** `run()` 은 가드가 막고 `resume()` 은 조용한 no-op. 일시적 API 실패 하나가 그 PR 의 리뷰를 영구히 반쪽으로 만든다 | **M8** — `retry_failed()` 를 계약에 더하거나 `status` 에 `partial` 을 여는 판단. 4-상태가 되면 M9 대시보드도 같이 바뀐다 |
  | **G-M6-10** | **`-confidence` 타이브레이커가 사실상 max 정책.** TODO ③ 이 "애그리게이터에 안 둔다"고 한 그 정책이 **대표를 고르는 방식으로 뒷문에** 들어와 있다 | **M8** — 게이트가 `sources` 전체의 confidence 를 직접 보면 닫힌다 |
  | **G-M6-11** | **merged 의 `line` 은 다수결이 아니라 대표의 것.** 넷이 `:17 :17 :15 :17` 일 때 대표가 testing 이면 `:15` 가 나간다. 아무도 안 재는 축이라 조용하다 | **M7** — GitHub 코멘트를 붙이면 그 좌표에 달린다. 진짜 답은 **줄 계산을 코드로 옮기는 것**(G-M0-1) |
  | **G-M6-12** | **`not_graded` 매칭이 `file` 을 안 본다.** 지어낸 파일의 `syntax-error` 도 면제된다. 그리고 `sample` 의 `not_graded` 가 **정답지가 정한 되돌림 조건(픽스처당 3개)을 이미 넘었다** | **다음 조각** — `aliases:` 를 열면서 같이. ⚠️ 자를 또 고치면 오늘 잰 값과 비교가 끊긴다 |
  | **G-M6-14** 🔴 | **`timeout=90` 은 전체 대기의 상한이 아니다.** httpx 는 `connect/read/write` 각각 90초이고 **`total` 필드가 없다** — `read` 는 한 번의 읽기 마감이라 서버가 조금씩 흘리면 매번 갱신된다. 목 서버 실측: **마감 2.0초 선언 → 77.5초 매달림.** `max_retries=1` 이 그걸 곱한다(9.1→18.1초). ⚠️ **`langgraph_engine.py` TODO ⑥ 이 (C)를 안 고른 근거**(*"SDK 마감이 안 먹히는 걸 본 적이 없다"*)**를 정확히 깬다.** ⚠️ 정직하게: 실제 90초로는 전형적 최악이 `90×2+백오프≈3분/에이전트` 이고 무한은 병리적 상대에서만. 공격자 입력이 아니라 열화된 릴레이가 필요하다 | **M4** — 워커가 붙어 사람이 Ctrl-C 못 할 때. 후보: `step_timeout` 정찰 · `concurrent.futures` 로 감싸기 · 총량 재는 트랜스포트 |
  | **G-M6-15** | **`demo_m5` 판정 ③이 진짜 경로를 한 줄도 안 밟는다.** `M5_DUMMY_AGENTS=all` 이라 `review_diff` 호출 **0회**(검증자 실측). hang 주입 자체가 더미 분기에서만 가능하다 — **그물이 걸린 곳과 매달릴 수 있는 곳이 다르다** | G-M6-14 와 같이. 바깥 마감이 생기면 그때 진짜 경로에 그물을 건다 |
  | **G-M6-16** | **`Finding.rationale` 에 `min_length` 가 없다.** `invariants.md` 는 INV-3 을 *"스키마가 거부하게 만드는 게 목표"* 라고 적었는데 빈 문자열이 통과한다. 데모 판정 ③은 고쳤지만(원본+merged 둘 다 검사) **스키마 층이 비어 있다** | **M6-3b 와 같이** — 스키마 수정은 프롬프트 수정이라 베이스라인이 리셋된다 |
  | **G-M6-13** | **`merged` 를 채점하는 출구가 없다.** `by:` 로 나눈 조각을 다시 합쳐 보는 자리가 비어 있다 — 시스템이 실제로 내놓는 것은 `merged` 인데 자는 관점별만 잰다 | **M6-3b 이후** — `eval_prompt` 에 merged 모드. 한 판에 API 4회라 K판 비용이 4배다 |

### M1 ~ M5 · M7 · M8

(각 마일스톤 착수 시 위 형식으로 추가)

---

## 완료 절차 (매 마일스톤 반복)

`docs/DONE.md`의 3단 판정을 그대로 따른다.

1. **데모 명령 실행** — 위 표의 명령을 그대로.
2. **독립 검증** — 새 세션 열고 이 프롬프트만 준다. **빌더 세션의 대화 내용은 절대 붙여넣지 않는다.**

   ```
   너는 독립 리뷰어다. 코드를 쓰지 마라. 빌더의 주장을 참이라 가정하지 마라.
   목표: <마일스톤 목표 한 문장>
   성공 기준: <데모 명령 + 통과 기준>
   불변식: docs/invariants.md
   직접 실행해서 확인하고, 무엇이 위험한지 말해라.
   ```

   성공 기준을 "돌아가나"가 아니라 **"어떤 불변식이 위험한가"**로 물을 것 [02:57:57].
3. **이해도 체크** — 세 질문에 내가 답한다. 답 못 하면 코드가 돌아도 미완료 [03:00:45].

## 마일스톤 종료 시 갱신할 파일

- `docs/PLAN.md` — 상태, 독립검증 결과, 알려진 구멍
- `docs/CURRENT.md` — 지금 뭐가 살아있고 뭐가 스텁인지
- `docs/adr/` — 되돌릴 수 있는 결정을 새로 내렸다면

이 셋을 갱신해야 다음 세션이 콜드 스타트할 수 있다. **상태는 컨텍스트 창이 아니라 디스크에 산다** [02:46:02].
