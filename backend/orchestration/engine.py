"""워크플로 엔진의 추상 계약.

이 파일에는 **LangGraph 라는 단어가 한 번도 안 나온다.** 그게 이 파일의 존재 이유다.

왜 추상부터 만드나
------------------
LangGraph 선택은 **되돌릴 수 있는 결정**이다 [01:38:27]. MVP 에 맞아서 골랐을 뿐,
Temporal 로 갈아탈 이유(내구성 보장, 엔터프라이즈 성숙도)가 생길 수 있다.
그때 **클래스 하나를 추가하는 것으로 끝나야** 한다 — 호출하는 쪽 코드는 안 고치고.

그래서 노출하는 것은 세 메서드뿐이다 [01:40:33]:

    run()        새 리뷰를 시작한다
    resume()     죽었다 살아난 뒤 이어서 돌린다
    get_state()  지금 어디까지 갔나

⚠️ 이 셋보다 많이 노출하면 추상이 새기 시작한다. LangGraph 의 `Send`, `Command`,
   `interrupt` 같은 개념이 시그니처에 나타나는 순간, 갈아탈 때 호출부까지 고쳐야 한다.

전체 그림에서 어디인가
----------------------
    PR → ① 웹훅 → ② 큐 → ③ 워커 → **④ 여기서 그래프가 돈다** → ⑤ 애그리게이터 → ⑥ 게이트

③ 워커가 큐에서 잡을 꺼내 `run()` 을 부른다. 워커가 죽으면 다음 워커가 `resume()` 을 부른다.
"""

from __future__ import annotations

from abc import ABC, abstractmethod
from typing import Any


class WorkflowEngine(ABC):
    """리뷰 워크플로를 돌리는 엔진. 구현체는 `langgraph_engine.py`."""

    # ─────────────────────────────────────────────────────────────
    # TODO(human) — 세 메서드의 시그니처를 정한다
    #
    # 아래 세 개의 `...` 자리를 채운다. 몸통은 `raise NotImplementedError` 가 아니라
    # `...` 그대로 둔다 (@abstractmethod 가 이미 강제한다).
    #
    # ── 왜 이게 판단인가 ─────────────────────────────────────────
    # 시그니처는 **계약**이다. 한 번 정하면 워커·구현체·테스트가 전부 여기 맞춰진다.
    # 그리고 이건 M8 까지 살아남는 유일한 인터페이스다 — 갈아타는 게 목적이니까.
    #
    # ── 결정 1: run() 은 무엇을 받나 ────────────────────────────
    #   (A) run(diff: str)                       — M0 의 review_diff 와 같은 모양
    #   (B) run(pr_payload: dict)                — 웹훅 body 를 통째로
    #   (C) run(review_id: ?, diff: str)         — 식별자를 같이 받는다
    #
    #   기준: **재개할 때 이 그래프를 무엇으로 다시 찾을 것인가.**
    #   resume() 은 "그 리뷰"를 지목할 수 있어야 하는데, (A) 에는 지목할 이름이 없다.
    #
    #   ⚠️ 이 식별자는 **DB 와 무관하다.** M5 에는 DB 가 안 들어온다
    #      (M3 record_event 도 M4 Redis 도 미뤘다 — `CURRENT.md` 참조).
    #      식별자가 필요한 이유는 저장이 아니라 **체크포인터** 다 —
    #      그래프 상태를 무엇으로 구분해 저장하고 다시 찾을 것인가.
    #      완료 판정 ②(`kill -9` 후 재개)가 이것 없이는 성립하지 않는다.
    #
    #   ↳ 그래서 따라오는 질문 둘:
    #      · **타입은?** DB 가 없으니 지금은 아무거나 될 수 있다 (`int` / `str` / UUID).
    #        나중에 `004_truth.sql` 의 `reviews.id`(BIGSERIAL → 정수)와 맞출 생각이면
    #        지금부터 정수로 두는 게 편하고, 그때 가서 매핑할 생각이면 문자열이 자유롭다.
    #        **어느 쪽이든 지금 정하는 이유가 있어야 한다** — "나중에 그럴 것 같아서"는 이유가 아니다.
    #      · **누가 만드나?** 지금은 호출자(워커 / 데모 스크립트)뿐이다. 엔진은 못 만든다 —
    #        만들 곳이 없으니까. 이게 이 인터페이스가 DB 를 모르는 상태로 남는 이유다.
    #
    # ── 결정 2: run() 은 무엇을 돌려주나 ────────────────────────
    #   (A) 최종 state (findings 까지 든 것)
    #   (B) 식별자 하나 (나중에 get_state 로 조회)
    #   (C) 아무것도 안 돌려준다 (DB 에만 쓴다)
    #
    #   기준: 워커가 이걸 받아서 뭘 하나? 그리고 **중간에 죽으면** 반환값이 존재하나?
    #
    # ── 결정 3: get_state() 가 돌려주는 것의 타입 ───────────────
    #   `dict[str, Any]` 로 열어둘지, 우리 타입으로 좁힐지.
    #   넓히면 구현체가 편하고, 좁히면 **추상이 안 샌다.**
    #   ⚠️ LangGraph 의 `StateSnapshot` 을 그대로 돌려주면 이 파일의 목적이 무너진다.
    #
    # 힌트: 세 메서드가 **같은 식별자 타입**을 쓰는지 확인할 것.
    #       run 이 A 를 돌려주는데 resume 이 B 를 받으면 그 사이를 누가 변환하나?
    # ─────────────────────────────────────────────────────────────

    @abstractmethod
    def run(self, pr_id: str,  diff: str) -> ...:
        """새 리뷰를 시작한다. 그래프를 처음부터 끝까지 돌린다."""

        return None 

    @abstractmethod
    def resume(self, agent_id) -> ...:
        """중단된 리뷰를 체크포인트에서 이어 돌린다.

        ⚠️ INV-2 가 여기 걸린다 — 이미 끝난 노드를 다시 호출하면
        같은 리뷰에 LLM 호출이 두 번 나간다. 체크포인터가 그걸 막아야 한다.
        """
        return None 

    @abstractmethod
    def get_state(self, ...) -> ...:
        """지금 어디까지 갔나. 대시보드와 워커 복구가 읽는다."""
        ...
