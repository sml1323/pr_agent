# CLAUDE.md — 이 프로젝트에서 일하는 방법

## 이게 뭘 만드는 물건인가

GitHub에 PR이 올라오면, AI 4개가 각자 다른 관점으로 코드를 읽고,
**확신 있는 지적만 자동으로 코멘트를 달고, 애매한 건 사람에게 넘기는 시스템.**

```
PR 열림 → ① 웹훅 수신 → ② Redis 큐 → ③ 워커
                                        ↓
              ④ security / quality / testing / docs  (4개 병렬)
                                        ↓
              ⑤ 애그리게이터 (중복 제거 + 전체 확신도)
                                        ↓
              ⑥ 게이트 ─ critical 있음    → 사람
                       ├ 확신 ≥ 0.6      → GitHub 자동 게시
                       └ 애매함           → 사람 큐
```

**제1원칙은 선별(selectivity)** [00:38:27] — 많이 지적하는 게 아니라 **틀린 말을 안 하는 것**이 목표.
그래서 모든 지적에 `confidence`와 `rationale`이 붙고, 마지막에 게이트가 있다.

마일스톤 ↔ 그림 매핑: M0=④의 조각 하나 · M1=① · M2=DB · M3=로그 · M4=② · M5=④배선 ·
M6=④⑤ 진짜 LLM · M7=RAG · M8=⑥ 게이트

---

## 역할 분담 ([ADR 0002](docs/adr/0002-implementation-mode.md), 2026-07-28 조정)

**Claude가 뼈대·배선·보일러플레이트를 쓴다. 설계 판단이 들어가는 자리만 `TODO(human)`으로 비운다.**
사용자가 채우고, Claude가 리뷰한다.

원래 ADR 0002는 "코드를 전혀 쓰지 않는다"였는데, 참고할 원본 코드가 없어서
(영상은 2시간 43분 설계만 하고 코드는 에이전트에게 시킨다 — 베낄 게 없음)
백지에서 시작하는 마찰이 학습을 막았다. 목적인 **설계 사고 60%**는 TODO 자리로 지킨다.

### 무엇을 TODO로 비우나

| 비운다 | 준다 |
|---|---|
| 스키마 필드의 타입·제약·enum 값 | import, 클래스 뼈대, 함수 시그니처 |
| 게이트·분기 조건 | 배선, 에러 처리, CLI 인자 파싱 |
| 프롬프트의 판단 기준 | 프롬프트의 골격 |
| 트러스트 바운더리 처리 | 설정 파일, 의존성 |

비운 자리마다 **왜 이게 판단인지 + 틀리면 뭐가 깨지는지**를 주석으로 남긴다. 답은 쓰지 않는다.

---

## 마일스톤 진행 순서 (매번 이대로)

0. **선행 학습 — 브리핑 → 읽기 → 만지기 → 적기 → 만들기** (2026-08-21 개정)

   파트(파일 하나 단위)를 시작하기 **전에** 개념을 통과시킨다. 순서가 중요하다.

   | | 도구 | 산출물 | 무엇을 하나 |
   |---|---|---|---|
   | **0-a 브리핑** | Claude가 채팅으로 | (없음) | **상황 → 왜 곤란한가 → 그래서 뭘 하고 싶은가 → 도구** 순서로 |
   | **0-b 읽기** | `/mattpocock-skills:teach` | `learning/lessons/NNNN-*.html` | 개념을 1차 출처 인용과 함께 읽는다. 퀴즈로 회상 연습 |
   | **0-c 만지기** | `/tycoon-sim` | `learning/sims/NNNN-*.html` | 같은 개념을 **직접 굴려본다**. 예측 → 실행 → 틀림 → 교정 |
   | **0-d 적기** | 사용자가 직접 | `learning/NOTEBOOK.md` | **레슨을 다시 보지 말고** 내 말로 3줄. 그다음 Claude가 검토 |
   | **0-e 만들기** | (1번부터) | 코드 + `TODO(human)` | 그제서야 코드 작업 |

   ⚠️ **0-a 를 건너뛰지 말 것** (2026-08-21 추가). Lesson 09·10·11 에서 연속으로
   "레슨이 이해 안 간다"가 나왔고, 세 번 다 **Claude 가 채팅에서 다시 푼 뒤에야** 붙었다.
   레슨 HTML 은 1차 출처 인용과 교차 링크가 촘촘해서 **나중에 다시 읽기엔 좋지만
   처음 이해하기엔 무겁다.** 그래서 역할을 갈랐다 — **이해는 브리핑이 만들고,
   레슨은 몇 주 뒤 돌아왔을 때 읽는 물건이다.**

   브리핑의 형식은 고정이다. 도구(어떻게)부터 꺼내면 매번 막힌다:

   1. **지금 우리 코드에서 벌어지는 일** — 구체적 장면 하나
   2. **왜 곤란한가** — 그 장면이 무엇을 깨뜨리나 (불변식·완료 판정과 연결)
   3. **그래서 뭘 하고 싶은가** — 한 문장
   4. **도구 / 갈림길** — 그제서야 API·문법·선택지

   ⚠️ **판단이 안 갈리는 자리에는 레슨을 붙이지 않는다.** 답이 사실상 하나면
   레슨은 근거가 아니라 숙제가 된다. 그럴 땐 브리핑만 하고 바로 코드로 간다.

   **0-d(적기) 를 건너뛰지 말 것.** 읽기와 만지기는 둘 다 *알아본* 것이고, 적기만이 *꺼낸* 것이다.
   못 쓰면 아직 안 배운 것이고, 그게 드러나는 게 이 단계의 값어치다.
   Claude는 사용자가 쓴 뒤에만 검토한다 — **먼저 답을 채워주면 이 단계가 통째로 무의미해진다.**

   ⚠️ **`/mattpocock-skills:teach`는 Claude의 스킬 목록에 뜨지 않는다.** 사용자가 슬래시로
   직접 불러야 로드된다. Claude는 개념 학습이 필요한 시점에 **이 명령을 치라고 안내**할 것.
   워크스페이스는 `learning/` (프로젝트 루트 아님 — `NOTES.md` 참조).

   **왜 두 번인가**: 레슨은 "왜 그런지"를 주고, 시뮬레이터는 "정말 그런지"를 준다.
   읽기만 하면 **유창성 착각**(fluency illusion)이 생긴다 — 읽을 땐 다 알겠는데
   손에 안 남는 것. 예측이 틀리는 경험이 저장 강도를 만든다.
   개념을 모르는 채로 `TODO(human)`을 채우면 판단이 아니라 찍기가 된다.

   **`/study`는 안 쓴다** — `learning/` 워크스페이스와 `study/` 디렉토리가 따로 생겨서
   학습 기록이 두 군데로 갈라진다. teach 하나로 통일.

1. **전체 그림에서 어디인지 먼저 말한다** — 위 다이어그램의 어느 번호인가.
   이걸 건너뛰면 사용자가 맥락 없이 필드를 채우게 되고, 실제로 그래서 실수가 나왔다.
2. **브리핑** — `WORKFLOW.md` ①의 5개 항목 (뭘/왜/순서/개념/불변식)
3. **Claude가 스캐폴드 + `TODO(human)`** — 한 번에 2~3개까지만. 더 비우면 백지와 같다.
4. **사용자가 채움** → 검증. "돌아갑니다"가 아니라 출력이 근거다.
   ⚠️ **데모 명령은 사용자가 직접 돌린다** (`WORKFLOW.md` ③). Claude의 Bash 출력은 사용자 화면에
   안 뜨므로 `! uv run python ...` 형태로 안내할 것. Claude가 대신 돌리는 건
   작은 단위 확인(스키마 제약이 실제로 거부하는지 등)까지만.
5. **자가 검증** (`PLAN.md`의 데모 명령) → **독립 검증** (새 세션) → 이해도 3문 → 문서 갱신

### 순차적으로 간다

한 번에 파일 하나, TODO 2~3개. 여러 마일스톤을 앞질러 설명하지 않는다.
결정도 마찬가지 — **필요해지기 직전에** 정한다 (저스트-인-타임).
`CURRENT.md`의 "결정을 미룬 것" 표가 그 시점을 관리한다.

---

## 세션 시작 시 읽는 순서

1. `docs/CURRENT.md` — 지금 뭐가 살아있고 다음 한 걸음이 뭔지
2. `docs/WORKFLOW.md` — 세션 운영 방법
3. `docs/invariants.md` — 절대 깨면 안 되는 4개
4. `docs/03-build-plan.md`의 **해당 마일스톤 절만** — 통째로 읽으면 이번 마일스톤이 묻힌다
5. `learning/NOTES.md` — 학습자에 대해 알아야 할 것 (백지 어려워함, 전체 그림 먼저 등)

`docs/02-architecture.md`는 "왜 이 모양이지?" 질문이 나올 때만.

### 오랜만에 돌아왔을 때 (복습 루프)

간격이 벌어지면 다 까먹는 게 정상이다. 이 순서로 되살린다 — Claude가 먼저 제안할 것:

1. **`learning/NOTEBOOK.md` 를 먼저 읽는다** — 레슨 5개를 다시 읽는 것보다 이 한 장이 빠르다.
   사용자가 자기 말로 적어둔 것이라 회복이 가장 빠른 경로다
2. `docs/CURRENT.md`의 "지금 살아있는 것" 표 — 어디까지 왔나
3. `learning/lessons/` 의 **관련 레슨을 브라우저로 연다** (`open` 명령).
   퀴즈가 붙어 있으므로 **답을 보기 전에 먼저 풀어본다** — 이게 회상 연습이다
4. 해당 개념의 `learning/sims/` 시뮬레이터가 있으면 한 번 굴려본다
5. 그다음 `TODO(human)`으로 복귀

레슨을 **다시 읽는 것보다 퀴즈를 먼저 푸는 게** 회복이 빠르다.
읽으면 "아 맞다" 하고 넘어가지만(유창성 착각), 풀면 뭘 잊었는지가 드러난다.

---

## 하지 말 것

- **`docs/DONE.md`와 `docs/invariants.md` 수정** — 사람만. 고칠 일이 생기면 사용자에게 말한다
- **답을 바로 주기** — 막혔을 땐 30분 룰. "이 에러 무슨 뜻이야?" → 원인만, 코드 말고
- **`0.6` 같은 정책 상수를 스키마·프롬프트에 넣기** — 스키마는 "무엇이 존재할 수 있나",
  정책은 "그중 뭘 통과시키나". 임계값은 M8 `backend/gate/`가 생길 때까지 어디에도 안 나온다
- **범위 확장** — M0~M8. "이왕 하는 김에" 금지. 늘리려면 사용자가 `DONE.md`를 고치고 ADR을 남긴다

## 디렉토리

`backend/` ↔ `frontend/`로 가른다 (`app/` 아님). FastAPI는 전체의 일부일 뿐이고
나중에 Next.js 대시보드가 같은 레포에 들어온다. 구조는 `03-build-plan.md:59`.

## 환경

`uv` 사용. 실행은 `uv run python ...`, 의존성 추가는 `uv add`.

### 깃 계정 — 커밋 전후로 확인한다

이 머신에 GitHub 계정이 **둘** 있다. 이 레포는 반드시 **`sml1323`** 으로 푸시한다.
확인 없이 밀면 **다른 계정 이름으로 커밋이 올라가고**, 원격이 public 이라 그대로 남는다.

커밋·푸시 때마다 네 단계를 지킨다:

```bash
gh auth status                       # ① 지금 누구인가 — 원래 계정을 기억해둔다
gh auth switch --user sml1323        # ② sml1323 이 아니면 전환 (맞으면 건너뜀)
git commit ... && git push           # ③ 작업
gh auth switch --user <원래 계정>     # ④ 끝나면 되돌린다
```

⚠️ **④를 빼먹지 말 것.** 이 레포 일이 끝난 뒤에도 `sml1323` 이 활성인 채로 남으면,
다음에 다른 레포에서 같은 사고가 **반대 방향으로** 난다. ①에서 원래 계정을 적어두는 게
④의 재료다 — 기억이 아니라 출력에서 읽는다.

`gh auth switch` 는 인자 없이 쓰면 계정이 둘일 때 다른 쪽으로 토글되지만,
셋 이상이면 선택 프롬프트가 떠서 멈춘다. **`--user` 를 명시할 것.**
